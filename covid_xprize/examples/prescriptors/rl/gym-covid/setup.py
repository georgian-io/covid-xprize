from setuptools import setup

setup(name='gym_covid',
      version='0.0.1',
      install_requires=['gym>=0.2.3']
)

setup(name='gym_covid_extra_states',
      version='0.0.1',
      install_requires=['gym>=0.2.3']
)
